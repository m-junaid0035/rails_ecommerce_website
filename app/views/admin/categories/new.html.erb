<% content_for :title, "New category" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl mb-6">New category</h1>

  <%= render "form", admin_category: @admin_category %>

  <div class="mt-4">
    <%= link_to "Back to categories", admin_categories_path, class: "w-full sm:w-auto text-center rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
  </div>
</div>
