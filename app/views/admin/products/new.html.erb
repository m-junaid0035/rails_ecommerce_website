<% content_for :title, "New product" %>

<div class="max-w-4xl mx-auto px-4 md:w-2/3 w-full">
  <h1 class="font-extrabold text-4xl mb-8 text-gray-900">New Product</h1>

  <%= render "form", admin_product: @admin_product %>

  <div class="mt-6">
    <%= link_to "Back to products", admin_products_path, 
        class: "w-full sm:w-auto text-center px-6 py-3 rounded-md bg-gray-300 hover:bg-gray-400 text-gray-800 font-semibold transition inline-block" %>
  </div>
</div>
